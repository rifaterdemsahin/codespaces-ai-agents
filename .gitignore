.DS_Store
*.log
.env
.env.*
!.env.example
devcontainer.zip
gitignore

# Agent / CLI local state (never commit). Keep project /start skill + commands.
.grok/*
!.grok/skills/
!.grok/skills/**
!.grok/commands/
!.grok/commands/**
.agy/
.local/

# Editor
.vscode/
.idea/

# Local test dumps (CI writes report.json into the Pages artifact only)
report.json
tests/last-run.json
__pycache__/
*.pyc

# Exported Termius key / WhatsApp zip — never commit
grok-idle
grok-idle.pem
grok-idle.txt
grok-idle-termius.zip
