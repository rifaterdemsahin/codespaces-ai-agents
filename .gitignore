.DS_Store
*.log
.env
.env.*
!.env.example
devcontainer.zip
gitignore

# Agent / CLI local state (never commit)
.grok/
.agy/
.local/

# Editor
.vscode/
.idea/

# Local test dumps (CI writes report.json into the Pages artifact only)
report.json
tests/last-run.json
